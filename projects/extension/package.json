{
  "name": "@kazeds/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Extension build - copy files to dist/'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268"
  }
}
